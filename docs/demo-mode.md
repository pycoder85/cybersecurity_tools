# Demo Mode

Driftwatch includes seeded demo data so the dashboard can be previewed without running a real scan.

```bash
driftwatch demo-data
driftwatch serve
```

Or combine it:

```bash
driftwatch serve --demo-data
```

The demo seed creates a host, multiple completed scans, findings, and evidence entries so every dashboard page renders meaningful content immediately.
