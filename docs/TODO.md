# Future Hardening TODOs

- Add signed rule-pack loading with rule version metadata.
- Add richer Windows event log abstractions with explicit privilege checks.
- Add per-finding analyst notes and status transitions.
- Add optional packaged binaries for Linux, macOS, and Windows.
- Add stricter file hash collection with bounded sampling and SHA-256 support.
- Add rule suppression and maintenance-window controls.
- Add local model integration behind the existing explanation provider interface.
- Add database migrations instead of metadata-only table creation.
- Add rate-limited background scanning and health reporting.
- Add richer export options including filters, evidence bundles, and signed reports.
- Add dashboard-side evidence export and downloadable report snapshots.
